const travel = require("./data/db.json");

export default function Tour(props) {
    return (
        <>
            <h1>Tours: </h1>
            {
            props.arrayOfData.map(object=>{
                return (
                    <
                )
            })
        }
        </>
    )
}
